name: 'Hello World JS Action'
description: 'Greet someone and record the time'
inputs:
  who-to-greet:
    description: 'Who to greet'
    required: true
    default: 'World'
outputs:
  time:
    descripton: 'The time we greeted you'
runs:
  using: 'node16'
  maind: 'index.js'
